package main;

public class BST {
	// se devuelve un int
	// mismo caso base que en el 7
	
	/*
	 * Antes de la llamada recursiva crear una variable. Hacer un if(almacenando el
	 * valor que lo cumpla en la variable que hemos creado), si no lo cumple la
	 * variable es 0
	 */
	
	//return par + recursive(current.left) + recursive(current.right);

}
