package main;

public class BST {
	//Si no hay nodo se devuelve 0 (if (current == null){ return 0; })
	//Se devuelve un int
	//return current.value + recursive(current.left) + recursive(current.right);

}
